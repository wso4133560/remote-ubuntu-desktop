# remote-ubuntu-desktop
此项目主要是用于web访问远程ubuntu客户端的简单实现

# 需要安装粘贴版
sudo apt install -y wl-clipboard xclip

# 运行命令
./start-server.sh

./start-frontend.sh

./start-client.sh


# 高性能客户端
./start-server.sh

./start-frontend.sh

RC_FORCE_REBUILD=1 ./start-client-cpp.sh


